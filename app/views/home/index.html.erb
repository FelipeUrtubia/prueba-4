<h1>Index de home!</h1>

<div class="container row">
  <% @todos.each do |product|%>
    <div class="col-sm-3">
      <div class="card">
        <%= image_tag product.photo%>
        <div class="card-body text-center">
          <h5 class="card-title">nombre de la tarea</h5>
          <p class="card-text">un poco de detalle</p>
          <%= link_to "#", class: "btn btn-success", method: :post do%>
            <i class="fas fa-shopping-cart"></i>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>

</div>
<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Photo</th>
      <th>Done?</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @todos.each do |todo| %>
      <tr>
        <td><%= todo.name %></td>
        <td><%= todo.description %></td>
        <td><%= image_tag (todo.photo)%></td>
        <td><%= todo.done? %></td>
        <td><%= link_to 'Show', todo %></td>
        <td><%= link_to 'Edit', edit_todo_path(todo) %></td>
        <td><%= link_to 'Destroy', todo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Todo', new_todo_path %> -->
